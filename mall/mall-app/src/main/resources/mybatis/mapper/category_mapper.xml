<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yue.infrastructure.dao.ICategoryDao">

    <resultMap id="dataMap" type="com.yue.infrastructure.dao.po.Category">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="code" property="code"/>
        <result column="icon_url" property="iconUrl"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="queryCategoryById" parameterType="java.lang.Integer" resultMap="dataMap">
        select id, name, code, icon_url, sort_order, status, create_time, update_time
        from category
        where id = #{id}
    </select>

    <select id="queryCategoryByCode" parameterType="java.lang.String" resultMap="dataMap">
        select id, name, code, icon_url, sort_order, status, create_time, update_time
        from category
        where code = #{code}
    </select>

    <select id="queryCategoryList" resultMap="dataMap">
        select id, name, code, icon_url, sort_order, status, create_time, update_time
        from category
        where status = 1
        order by sort_order asc, id asc
    </select>

    <select id="queryCategoryListAll" resultMap="dataMap">
        select id, name, code, icon_url, sort_order, status, create_time, update_time
        from category
        order by sort_order asc, id asc
    </select>

    <insert id="insert" parameterType="com.yue.infrastructure.dao.po.Category" useGeneratedKeys="true" keyProperty="id">
        insert into category (name, code, icon_url, sort_order, status)
        values (#{name}, #{code}, #{iconUrl}, #{sortOrder}, #{status})
    </insert>

    <update id="update" parameterType="com.yue.infrastructure.dao.po.Category">
        update category
        set name = #{name}, code = #{code}, icon_url = #{iconUrl}, sort_order = #{sortOrder}, status = #{status}
        where id = #{id}
    </update>

    <delete id="deleteById" parameterType="java.lang.Integer">
        delete from category where id = #{id}
    </delete>
</mapper>
