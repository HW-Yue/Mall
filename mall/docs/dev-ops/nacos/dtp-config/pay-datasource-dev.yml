# pay 服务 Hikari 连接池可调项（dev）
# DataId: pay-datasource-dev.yml  Group: DEFAULT_GROUP
# pool-name = Pay_HikariCP（命名见根 CLAUDE.md，ops-agent-spring-ai 在 hikari SOP 中按 pool→application 反查）

spring:
  datasource:
    hikari:
      maximum-pool-size: 25
      minimum-idle: 15
