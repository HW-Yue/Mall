# pay：运行时配置（非 DynamicTp 线程池配置）
# 本地：由 application-dev.yml / application-test.yml 通过 spring.config.import classpath 加载。
# Nacos：发布为 DataId「pay-service-runtime-dev.yml」、Group「DEFAULT_GROUP」。

logging:
  level:
    root: info
