<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.bugstack.infrastructure.dao.IUserDao">

    <resultMap id="dataMap" type="cn.bugstack.infrastructure.dao.po.UserAccount">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="openid" property="openid"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="queryByOpenid" resultMap="dataMap">
        select id, username, password, openid, create_time, update_time
        from user_account
        where openid = #{openid}
    </select>

    <select id="queryByUsername" resultMap="dataMap">
        select id, username, password, openid, create_time, update_time
        from user_account
        where username = #{username}
    </select>

    <insert id="insert" parameterType="cn.bugstack.infrastructure.dao.po.UserAccount">
        insert into user_account (username, password, openid, create_time, update_time)
        values (#{username}, #{password}, #{openid}, now(), now())
    </insert>

</mapper>
