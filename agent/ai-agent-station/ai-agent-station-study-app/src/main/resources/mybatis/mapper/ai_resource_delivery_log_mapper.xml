<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.bugstack.ai.infrastructure.dao.IAiResourceDeliveryLogDao">

    <resultMap id="AiResourceDeliveryLogMap" type="cn.bugstack.ai.infrastructure.dao.po.AiResourceDeliveryLog">
        <id column="id" property="id"/>
        <result column="order_id" property="orderId"/>
        <result column="user_id" property="userId"/>
        <result column="biz_type" property="bizType"/>
        <result column="biz_id" property="bizId"/>
        <result column="goods_type" property="goodsType"/>
        <result column="res_key" property="resKey"/>
        <result column="res_value" property="resValue"/>
        <result column="unit" property="unit"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" parameterType="cn.bugstack.ai.infrastructure.dao.po.AiResourceDeliveryLog" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ai_resource_delivery_log (
            order_id, user_id, biz_type, biz_id, goods_type, res_key, res_value, unit, status, create_time, update_time
        ) VALUES (
            #{orderId}, #{userId}, #{bizType}, #{bizId}, #{goodsType}, #{resKey}, #{resValue}, #{unit}, #{status}, now(), now()
        )
    </insert>

    <select id="queryByOrderId" parameterType="java.lang.String" resultMap="AiResourceDeliveryLogMap">
        SELECT id, order_id, user_id, biz_type, biz_id, goods_type, res_key, res_value, unit, status, create_time, update_time
        FROM ai_resource_delivery_log
        WHERE order_id = #{orderId}
    </select>

    <select id="queryByUserId" parameterType="java.lang.String" resultMap="AiResourceDeliveryLogMap">
        SELECT id, order_id, user_id, biz_type, biz_id, goods_type, res_key, res_value, unit, status, create_time, update_time
        FROM ai_resource_delivery_log
        WHERE user_id = #{userId}
        ORDER BY create_time DESC
    </select>

    <select id="queryPageByUserId" resultMap="AiResourceDeliveryLogMap">
        SELECT id, order_id, user_id, biz_type, biz_id, goods_type, res_key, res_value, unit, status, create_time, update_time
        FROM ai_resource_delivery_log
        WHERE user_id = #{userId}
        <if test="bizType != null and bizType != ''">
            AND biz_type = #{bizType}
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
        ORDER BY create_time DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByUserId" resultType="long">
        SELECT COUNT(1)
        FROM ai_resource_delivery_log
        WHERE user_id = #{userId}
        <if test="bizType != null and bizType != ''">
            AND biz_type = #{bizType}
        </if>
        <if test="status != null">
            AND status = #{status}
        </if>
    </select>

    <update id="updateStatusByOrderId">
        UPDATE ai_resource_delivery_log
        SET status = #{status}, update_time = now()
        WHERE order_id = #{orderId}
    </update>

</mapper>

