# group-buy-service Hikari 连接池可调项（dev）
# DataId: group-buy-service-datasource-dev.yml  Group: DEFAULT_GROUP

spring:
  datasource:
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
